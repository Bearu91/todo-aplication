{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edycja</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" class="btn btn-success" value="Edycja" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('todo_index') }}">Wróć do listy</a>
        </li>
    </ul>
{% endblock %}
